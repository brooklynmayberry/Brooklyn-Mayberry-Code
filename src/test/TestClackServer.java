package test;

import main.*;

/**
 * A class to test the Clack Server
 *
 * @author Benjamin T. Mustico and Brooklyn Mayberry
 * @version 1 October, 2022
 *
 */
public class TestClackServer {

    public static void main(String[] args) {
        /*

        //Testing the constructors
        ClackServer server1 = new ClackServer(100);
        ClackServer server2 = new ClackServer(100);
        ClackServer server3 = new ClackServer();

        //Testing the equals method (they are equal)
        if( server1.equals(server2)) {
            System.out.println("They are equal!");
        }
        else {
            System.out.println("they are NOT equal!");
        }

        //Testing the equals method (they are NOT equal)
        if( server1.equals(server3)) {
            System.out.println("They are equal!");
        }
        else {
            System.out.println("they are NOT equal!");
        }

        //Testing the hashCode() method
        System.out.println(server1.hashCode());
        System.out.println(server2.hashCode());
        System.out.println(server3.hashCode());
        System.out.println();

        //Testing the toString() method
        System.out.println(server1);
        System.out.println(server3);

        //Testing the accessor methods
        System.out.println(server1.getPort());
        System.out.println(server3.getPort());

        //Testing the methods that haven't been implemented yet
        server1.start();

        server1.receiveData();

        server1.sendData();

        // Ensures the code ran through
        System.out.println();
        System.out.println("The code ran without a fatal error");

         */
    }

}
